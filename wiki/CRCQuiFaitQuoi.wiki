#labels Analyse-CRC
= CRC Qui fait quoi = 
(pour mercredi le 16)

Voici la distribution de CRC a faire, choisissez les CRC du groupe *non assigné* puis copiez leur ligne sous votre nom si vous voulez (et pouvez) le faire.
Pour chacun d'entre vous j'ai créé une page vous permettant de travailler sur vos CRC, le lien est sur votre nom.
Dans chacune de ses page un layout de CRC est présent (visible seulement en mode édition) svp l'utiliser pour faire les vôtres.
Les groupe de classe marqué (de base) signifie par exemple pour un module que l'on montre la structure "de base" de tout module qui puisse existé et non pas le CRC d'un module Finance ou Employer, certain module ou viewer doivent etre _CRCiser_ puisque ce sont des module ou des vue obligatoire au fonctionnement de l'application

*ATTENTION* c'est une liste de *groupe de classe* et non pas une liste de classe, un groupe peu donc se traduire par plusieurs CRC, il n'est pas néscéssaire de séparé les groupe en CRC sur cette page-ci.
Bon travail.

3 statuts possibles (inactif, en cour, fini)

*Consultez le diagramme notre projet pour savoir en gros comment les classes interagissent avec les autres*
[http://newtonerp.googlecode.com/files/Diagramme%20ERP%20Web.class.violet]

== non assigné ==

|| *Groupe de classe* || *statut* ||
|| Entity (de base) || inactif ||
|| OrmisableEntity || inactif ||
|| ViewableEntity (de base) || inactif ||
|| SpecilizedViewer (de base) || inactif ||
|| Module (de base) || inactif ||
|| MainModule _(modèle du module du menu principal)_ || inactif ||
|| UserRightModule || inactif ||
|| Action _(gros morceau à conceptualiser)_ || inactif ||
|| EventListener || inactif ||

== [GuillaumeCRC Guillaume] ==

|| *Groupe de classe* || *statut* ||
|| Specification || fini ||
|| Task || fini ||
|| TaskManager || fini ||
|| TaskViewer || fini ||
|| TaskModule || fini ||

== [KovalevCRC Kovalev] ==

|| *Groupe de classe* || *statut* ||
|| DataOrm || inactif ||
|| Viewer (main Viewer) _(une page de servlet qui envoie des HTTP selon ce que l'utilisateur fait)_ || inactif ||
|| HttpListener _(une page de servlet qui reçoit la requête HTTP)_ || inactif ||
|| HttpRequest _(contient la requête envoyée au servlet, pas forcément une vraie classe qu'on va créer, peut-être classe fournie dans l'API des servlet alors faire en dernier)_ || inactif ||

Servelet correspond aux classes:
HttpListener, HttpRequest et Viewer
Voir pages pour info sur servlet
[http://www.exampledepot.com/egs/javax.servlet/com_mycompany_MyServlet.html]
[http://java.sun.com/products/servlet/articles/tutorial/]

== [JonatanCRC Jonatan] ==

|| *Groupe de classe* || *statut* ||

== [GabrielCRC Gabriel] ==

|| *Groupe de classe* || *statut* ||

== [PascalCRC Pascal] ==

|| *Groupe de classe* || *statut* ||